Three fixes.

═══ 1. ANTIGUA CONTENT ═══
The "Trade Wind Passage" entry under Antigua is broken/wrong — remove it entirely and
replace with a different, real Antigua attraction. Use well-known verifiable ones, e.g.
Nelson's Dockyard (English Harbour), Shirley Heights Lookout, Dickenson Bay, Devil's Bridge,
Half Moon Bay, or Stingray City. Pick one that isn't already listed on the page.

Since these islands are real places, all content must be factually accurate — no invented
landmarks, no made-up history, no fabricated hours or prices. Stick to widely-documented,
uncontroversial descriptions of what the place is. If you can't state something confidently,
leave it out rather than guessing. Audit the OTHER islands' entries for the same problem and
fix any that are garbled, duplicated, or not real.

═══ 2. DESTINATION MUST ACTUALLY PRESET ═══
This is currently not working and it must. Every one of these sets the destination:
  - "Book a ride to [Island]" on an island page
  - a dock-level "Book a ride here" button (sets island AND dock)
  - clicking an island card or map shape and then booking from that page

Implementation: /book?destinationIsland=<id>&destinationDock=<id optional>
On mount, read the params BEFORE first render of the form, validate the island exists and
the dock belongs to it, and write them into booking state. Show a pill at the top:
  "Going to Antigua · [dock]   [Change]"
Then ask for pickup only, and jump straight to the review step once pickup is chosen.
The pickup island list must exclude the destination island.
If the param is invalid, fall back to the normal flow silently — never crash, never leave a
half-filled form.
Also make sure the router doesn't remount and wipe state, and that a hard refresh on the
deep-linked URL preserves the destination.

VERIFY EXPLICITLY: click through all 7 islands → the destination pill shows the correct
island every time, and the trip that gets created has that island as its destination.

═══ 3. REALISTIC FARES ═══
Current pricing is wrong (~$181 for a 26 km hop) and the fare isn't shown until AFTER
booking, which contradicts the "upfront pricing" claim on the landing page.

New model, in one shared pricing.ts used by both the estimate and the booking:
  baseFare      = $8.00
  perKm         = $1.60
  classMultiplier: catamaran 0.8, water taxi 1.0, cruiser 1.3, speedboat 1.6
  passengers    = 1st at full price, each additional at 75%
  fare = (baseFare + perKm × km) × classMultiplier × passengerFactor
  round to the nearest $0.50, display with 2 decimals
Sanity check the output: a ~26 km water taxi for 2 people should land near $88, a short
under-10 km hop near $25-40. Adjust perKm if the numbers come out unreasonable — a boat
crossing should read like a ferry or water taxi fare, not a charter.

Show the fare BEFORE booking:
  - on the review step, as a line-item breakdown: base, distance (with km), boat class,
    additional passengers, total
  - update it live when passenger count or boat class changes
  - on island pages, a "from $X" estimate next to each dock's book button, using a water
    taxi for 1 passenger from the nearest island
  - keep it on the trip page and in the logbook, matching to the cent